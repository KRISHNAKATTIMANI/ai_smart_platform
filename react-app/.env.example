# Frontend Environment Variables

# Development (uses proxy in package.json)
# No need to set anything - uses proxy to localhost:5000

# Production (Render.com)
REACT_APP_API_URL=https://ai-assistant-api.onrender.com

# Replace with your actual Render backend URL after deployment
